#!/usr/bin/env bash
set -euo pipefail

if [[ "${BR_REVIEW_USE_CLI_FLOW:-}" != "1" ]]; then
  echo "❌ Commit blocked: use CLI flow wrapper"
  echo "Run: bun run flow:commit -- -m \"type(scope): message\""
  exit 1
fi

bunx lint-staged
